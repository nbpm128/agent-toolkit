# {{PLUGIN_NAME}}

{{ONE_LINE_DESCRIPTION}}

## What's inside

<!-- List the components this plugin ships. Remove rows that don't apply. -->

- **Skills**: `{{skill-name}}` — {{what it does}}
- **MCP servers**: `{{server-name}}` ({{stdio|streamable-http|sse}}) — {{what it connects to}}

## Install

<!-- See references/install.md in plugin-creator for the full per-client matrix.
     Replace the URL/path below. -->

```bash
# from a git repo (session):
claude --plugin-url {{GIT_URL}}

# from a local directory (session):
claude --plugin-dir {{LOCAL_PATH}}
```

## Requirements

<!-- Anything a user must install separately, e.g. a runtime for a bundled stdio
     MCP server. Remove if none. -->

- {{requirement}}

## License

{{LICENSE}}
